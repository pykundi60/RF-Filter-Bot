version: "3.9"
services:
  worker:
    build: .
    environment:
      API_ID: $API_ID
      API_HASH: $API_HASH
      BOT_TOKEN: $BOT_TOKEM
      DATABASE_URL: $DATABASE_URL
      CHANNELS: $CHANNELS
      DATABASE_NAME: $DATABASE_NAME
      ADMINS: $ADMINS
      LOG_CHANNEL: $LOG_CHANNEL
